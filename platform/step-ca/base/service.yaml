apiVersion: v1
kind: Service
metadata:
  name: step-ca
  namespace: step-ca
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: step-ca
  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 9000
